export class <%= entityNameTitleCase %>Model {
    <%= entityNameLowerCase %>_id: number;
  <% properties.forEach(prop => { -%>
    <%= prop.prop %>: <%= prop.type  %>;
<% }) -%>
    createdAt: Date;
    updatedAt: Date;
  }
